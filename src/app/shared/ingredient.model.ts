export class Ingredients{
    // //define properties
    // public name:string;
    // public amount:number;

    // //class constructor to initialize the properties 
    // constructor(name:string, amount:number){
    //         this.name = name;
    //         this.amount = amount;
    // }

    constructor(public name:string, public amount:number){} //shortcut
}